# Optional. Copy to .env before running docker compose.

# Signing key for JWTs. Any length — short keys are stretched to the HS256 minimum.
JWT_KEY=replace-with-a-long-random-secret

# Groq API key. The chatbot returns a clear 503 while this is empty; everything else works.
GROQ_API_KEY=
