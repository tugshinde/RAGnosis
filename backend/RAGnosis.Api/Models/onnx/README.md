Place the sentence-transformer ONNX model here:

  all-MiniLM-L6-v2.onnx
  vocab.txt

Until both files are present the embedding layer reports itself unavailable
and knowledge retrieval falls back to keyword matching.
